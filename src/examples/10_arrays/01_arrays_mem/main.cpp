#include "arrays_mem.h"

int main() 
{

	//use_stack_array();
	//array_months();
	//arrays_and_pointers();
	const auto SIZE = 4;
    int numbers[SIZE]{1,2,3, -1};

	//display_array(numbers);
	display_array(numbers, SIZE);

	return 0;

}